import { Routes } from '@angular/router';
import { FormulasComponent } from './practica1/formulas/formulas.component';

export const routes: Routes = [
    {
        path: '',
        component: FormulasComponent
    }
];
